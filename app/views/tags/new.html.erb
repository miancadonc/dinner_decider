<%= form_with model: @tag, local: true do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "#{@tag.errors.messages[:name].first}" %>

    <%= f.submit"Create New Tag" %>
<% end %>