<%= form_with model: @ingredient, local: true do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "#{@ingredient.errors.messages[:name].first}" %>

    <%= f.submit"Create New Ingredient" %>
<% end %>